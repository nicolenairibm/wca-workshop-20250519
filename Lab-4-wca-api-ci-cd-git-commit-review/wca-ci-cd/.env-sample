# Required: Your smee.io URL for webhook forwarding
export SMEE_URL='https://smee.io/<example unique url>'

# Required: GitHub personal access token for API access
export GITHUB_TOKEN='<github token for the personal repository to monitor ci cd>'

# Required: Watson Code Assistant API key
export IAM_APIKEY='wca api key from the techzone account'

# Optional: Webhook secret for added security
export GITHUB_WEBHOOK_SECRET='<github weebhook secrent for the personal repository>'